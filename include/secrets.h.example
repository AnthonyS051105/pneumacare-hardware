#pragma once

// Template — salin file ini menjadi secrets.h dan isi nilai sebenarnya.
// secrets.h TIDAK boleh di-commit (lihat .gitignore).

#define WIFI_SSID "..."
#define WIFI_PASSWORD "..."
#define DEVICE_API_TOKEN "..."
